/**
 * Centralized configuration exports
 * All configuration should be imported from here for consistency
 */

export { env, config } from './env';

// Re-export for backward compatibility
export { config as default } from './env';
